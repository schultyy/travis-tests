language: ruby
dist: precise
script: true
before_deploy:
  - gem install --no-rdoc --no-ri fpm
  - gem which fpm
  - echo $PATH
  - which -a fpm
deploy:
  provider: script
  script: echo which -a fpm
skip_cleanup: true
on:
  all_branches: true
